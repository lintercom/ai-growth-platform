{
  "name": "ai-growth-platform",
  "version": "0.1.0",
  "description": "AI Growth & Design Platform - Toolkit a CLI pro weby a e-shopy",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r --filter './packages/*' dev",
    "clean": "pnpm -r --filter './packages/*' clean && rm -rf node_modules",
    "lint": "pnpm -r --filter './packages/*' lint",
    "test": "pnpm -r --filter './packages/*' test",
    "typecheck": "pnpm -r --filter './packages/*' typecheck",
    "aig": "node packages/aig-cli/dist/bin/aig.js",
    "prepublishOnly": "pnpm -r run build"
  },
  "keywords": [
    "ai",
    "openai",
    "seo",
    "design",
    "architecture",
    "cli",
    "monorepo"
  ],
  "author": "",
  "license": "MIT",
  "packageManager": "pnpm@8.15.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}
